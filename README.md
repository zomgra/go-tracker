# go-bitbucket Mykyta Serdiuk

For start program use:

1. `make db`

2. `make migration`

3. `make run`


Listener uri:

`localhost:8000/api/shipment/{barcode}` -- GET
`localhost:8000/api/shipment?quantity=X` -- POST